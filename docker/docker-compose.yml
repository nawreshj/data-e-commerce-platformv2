services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ../e-commerce-monitoring/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - ecommerce-net

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - ecommerce-net

  ms-membership:
    image: newsagesse/ecommerce-membership:1.0
    container_name: ms-membership
    ports:
      - "8081:8081"
    networks:
      - ecommerce-net

  ms-product:
    image: newsagesse/ecommerce-product:1.0
    container_name: ms-product
    ports:
      - "8082:8082"
    networks:
      - ecommerce-net
    depends_on:
      - ms-membership

  ms-order:
    image: newsagesse/ecommerce-order:1.0
    container_name: ms-order
    ports:
      - "8083:8083"
    networks:
      - ecommerce-net
    depends_on:
      - ms-membership
      - ms-product

volumes:
  grafana-data:
  prometheus-data:

networks:
  ecommerce-net:
    driver: bridge
